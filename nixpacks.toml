[build]
builder = "nixpacks"
buildCommand = "npm ci && composer install --no-dev --optimize-autoloader --no-scripts && npm run build"

[deploy]
startCommand = "php -S 0.0.0.0:$PORT -t public"
healthcheckPath = "/"
healthcheckTimeout = 100